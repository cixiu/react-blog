{
  "apps": [
    {
      "name": "blog",
      "script": "server.prod.js",
      "env": {
        "COMMON_VARIABLE": "true"
      },
      "env_production" : {
        "NODE_ENV": "production"
      }
    }
  ],
  "deploy": {
    "production": {
      "user": "cc_manager",
      "host": ["120.78.196.10"],
      "port": "43960",
      "ref": "origin/master",
      "repo": "git@gitee.com:cixiu/react-blog.git",
      "path": "/www/blog/production",
      "ssh_options": "StrictHostKeyChecking=no",
      "post-deploy": "npm install --registry=http://registry.npm.taobao.org && npm run build && pm2 startOrRestart ecosystem.json --env production",
      "env": {
        "NODE_ENV": "production"
      }
    }
  }
}
